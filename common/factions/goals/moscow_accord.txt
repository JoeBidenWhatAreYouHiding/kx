faction_manifest_masters_of_eurasia = {
	name = faction_manifest_masters_of_eurasia_name
	description = faction_manifest_masters_of_eurasia_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:all_russian_ambitions
		completed_amount_collection = collection:all_owned_russian_ambitions

		scale = {
			consumer_goods_factor = -0.20
			industrial_capacity_factory = 0.10
			war_support_factor = 0.10
		}

		progress_sections = {
			manifest_fulfilled = {
				min = 0.75

				modifier = {
					resistance_target = -0.05
					justify_war_goal_time = -0.25
					war_stability_factor = 0.10
				}
			}
		}
	}
}
faction_manifest_world_communism = {
	name = faction_manifest_world_communism_name
	description = faction_manifest_world_communism_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = game:all_countries
		completed_amount_collection = collection:communist_countries

		scale = {
			army_attack_against_major_factor = 0.10
			land_reinforce_rate = 0.10
			consumer_goods_factor = -0.20
			conscription_factor = 0.10
		}

		progress_sections = {
			manifest_fulfilled = {
				min = 0.75

				modifier = {
					production_factory_max_efficiency_factor = 0.05
					syndicalist_drift = -0.05
					radical_socialist_drift = -0.05
					social_democrat_drift = -0.05
					social_liberal_drift = -0.05
					market_liberal_drift = -0.05
					social_conservative_drift = -0.05
					authoritarian_democrat_drift = -0.05
					paternal_autocrat_drift = -0.05
					national_populist_drift = -0.05
				}
			}
		}
	}
}
faction_goal_RUS_start_rearmament = {
	name = goal_RUS_start_rearmament_name
	description = goal_RUS_start_rearmament_desc
	category = short_term
	group = FOCUS_FILTER_INTERNAL_AFFAIRS
	
	allowed = { original_tag = RUS }
	
	completed = {
		num_of_military_factories > 45	
	}
	complete_effect = {
		add_faction_power_projection = 50
		add_faction_initiative = 1
	}
	ai_will_do = { base = 100 }
}
faction_goal_RUS_help_serbia = {
	name = goal_RUS_help_serbia_name
	description = goal_RUS_help_serbia_desc
	category = medium_term
	group = FOCUS_FILTER_POLITICAL
	
	allowed = { original_tag = RUS }
	visible = { has_completed_focus = RUS_Russo-Serbian_Friendship }
	
	completed = {
		SER = { 
			is_ally_of_ROOT = yes 
			if = {
				limit = { has_completed_focus = SER_slavic_federation }
				all_state = {
					OR = {
						region = 143 #Northern Balkans
						state = 102 #Slovenia
						state = 1101 #Slovenia
						state = 1103 #Slovenia
						state = 1104 #Slovenia
					}
					is_owned_by_ROOT_or_ally = yes
				}
			}
			else = {
				45 = { is_owned_by_ROOT_or_ally = yes }
				105 = { is_owned_by_ROOT_or_ally = yes }
				1097 = { is_owned_by_ROOT_or_ally = yes }
				794 = { is_owned_by_ROOT_or_ally = yes }
				1128 = { is_owned_by_ROOT_or_ally = yes }
				875 = { is_owned_by_ROOT_or_ally = yes }
			}
		}
	}
	complete_effect = {
		add_faction_power_projection = 50
		add_faction_initiative = 1
	}
	ai_will_do = { base = 100 }
}
faction_goal_RUS_restore_1925_borders = {
	name = goal_RUS_restore_1925_borders_name
	description = goal_RUS_restore_1925_borders_desc
	category = medium_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { original_tag = RUS }
	
	completed = {
		218 = { is_owned_by_ROOT_or_ally = yes }
		234 = { is_owned_by_ROOT_or_ally = yes }
		237 = { is_owned_by_ROOT_or_ally = yes }
		810 = { is_owned_by_ROOT_or_ally = yes }
		408 = { is_owned_by_ROOT_or_ally = yes }
	}
	complete_effect = {
		add_faction_power_projection = 150
		add_faction_initiative = 1
	}
	ai_will_do = { base = 100 }
}
faction_goal_RUS_form_the_ussr = {
	name = goal_RUS_form_the_ussr_name
	description = goal_RUS_form_the_ussr_desc
	category = medium_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { original_tag = RUS }
	visible = { has_country_flag = soviet_russia_restored }
	
	completed = {
		has_completed_focus = RUS_Form_The_USSR
	}
	complete_effect = {
		add_faction_power_projection = 150
		add_faction_initiative = 1
	}
	ai_will_do = { base = 100 }
}
faction_goal_RUS_defeat_germany = {
	name = goal_RUS_defeat_germany_name
	description = goal_RUS_defeat_germany_desc
	category = long_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { original_tag = RUS }
	
	completed = {
		OR = {
			64 = { is_owned_by_ROOT_or_ally = yes }
			NOT = { country_exists = GER }
			GER = { has_capitulated = yes }
		}
	}
	complete_effect = {
		add_faction_power_projection = 400
		add_faction_initiative = 1
		custom_effect_tooltip = tooltip_white_line
		every_faction_member = {
			limit = { 
				is_subject = no
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = { base = 100 }
}
faction_goal_RUS_defeat_internationale = {
	name = goal_RUS_defeat_internationale_name
	description = goal_RUS_defeat_internationale_desc
	category = long_term
	group = FOCUS_FILTER_ANNEXATION
	
	allowed = { always = no }
	
	completed = {
		collection:european_countries = { has_socialist_government = no }
	}
	complete_effect = {
		add_faction_power_projection = 350
		add_faction_initiative = 1
		custom_effect_tooltip = tooltip_white_line
		every_faction_member = {
			limit = { 
				is_subject = no
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = { base = 100 }
}
faction_goal_RUS_defeat_japan = {
	name = goal_RUS_defeat_japan_name
	description = goal_RUS_defeat_japan_desc
	category = long_term
	group = FOCUS_FILTER_NAVY_XP
	
	allowed = { original_tag = RUS }
	
	completed = {
		OR = {
			JAP = { is_subject = yes }
			AND = {
				alliance_naval_strength_ratio > 2
				has_war_with = JAP
			}
		}
	}
	complete_effect = {
		add_faction_power_projection = 300
		custom_effect_tooltip = tooltip_white_line
		custom_effect_tooltip = INFLUENCE_TOP_TWO
		every_faction_member = {
			limit = { 
				faction_influence_rank < 3 
				is_subject = no
			}
			add_faction_initiative = 1
		}
		custom_effect_tooltip = ALL_FACTION_MEMBERS
		every_faction_member = {
			random_navy_leader = {
				tooltip = ONE_ADMIRAL
				visible_when_empty = yes
				limit = {
					NOT = { has_trait = seawolf }
					NOT = { has_trait = battleship_adherent }
				}
				gain_xp = 400
				add_coordination = 1
				add_trait = { trait = battleship_adherent }
			}
			add_mastery = {
				amount = 100
				track = capital_ships
			}
			add_tech_bonus = {
				name = goal_RUS_defeat_japan_name
				bonus = 1
				uses = 1
				category = naval_equipment
			}
		}
	}
	ai_will_do = {
		base = 0
		modifier = {
			factor = 0
			JAP = {
				is_in_faction_with = ROOT
				is_subject = no
			}
		}
		modifier = {
			add = 1 # Don't bother unless you're somewhat likely to be able to do it
			OR = {
				alliance_naval_strength_ratio > 0.6
				JAP = { is_subject = yes }
			}
		}
		modifier = {
			add = 1 # If you got a bunch of dockyards odds of succeeding go up + indication nation is gunning for navy
			num_of_naval_factories > 29
		}
	}
}
