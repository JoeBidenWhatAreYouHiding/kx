ROM_civil_war = {
	ROM_call_to_arms = {

		icon = generic_prepare_civil_war

		available = {
			has_war_with = ROM
			has_idea = ROM_for_the_king_idea
		}

		fire_only_once = yes

		days_remove = 60

		cost = freedec?0

		modifier = {
			mobilization_speed = 0.1
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_call_to_arms"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ROM_call_to_arms"
			add_war_support = 0.05
		}

		ai_will_do = {
			factor = 100
		}
	}

	ROM_last_stand = {

		icon = generic_prepare_civil_war

		available = {
			has_war_with = ROM
			NOT = { controls_state = 79 } # Bessarabia
		}

		visible = {
			NOT = { controls_state = 79 }
		}

		fire_only_once = yes

		modifier = {
			defence = 0.2
			dig_in_speed_factor = 0.5
		}

		days_remove = 45
		days_re_enable = 360

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_last_stand"
		}

		ai_will_do = {
			factor = 10
		}
	}
}

ROM_white_terror = {
	ROM_white_terror_ban_people_tribunals = {

		icon = generic_civil_support

		visible = {
			tag = ROM
		}

		fire_only_once = yes

		cost = 50

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_white_terror_ban_people_tribunals"
			ROM_decrease_chaos_by_2 = yes
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 10
				check_variable = { chaos > 3 }
			}

			modifier = {
				factor = 10
				check_variable = { chaos > 6 }
			}
		}
	}

	ROM_white_terror_ban_people_militias = {

		icon = generic_civil_support

		fire_only_once = yes

		days_remove = 14

		cost = 50

		modifier = {
			war_support_factor = -0.1
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_white_terror_ban_people_militias"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ROM_white_terror_ban_people_militias"
			ROM_decrease_chaos_by_2 = yes
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 10
				check_variable = { chaos > 3 }
			}

			modifier = {
				factor = 10
				check_variable = { chaos > 6 }
			}
		}
	}

	ROM_open_internment_camps = {

		icon = generic_decision

		available = {
			custom_trigger_tooltip = {
				tooltip = ROM_open_internment_camps_tooltip
				OR = {
					has_idea = ROM_chaos_medium
					has_idea = ROM_chaos_high
					has_idea = ROM_anarchy
				}
			}
		}

		fire_only_once = yes

		cost = 25

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_open_internment_camps"
			set_country_flag = opened_internment_camps
			ROM_decrease_chaos_by_2 = yes
		}

		ai_will_do = {
			factor = 5

			modifier = {
				factor = 10
				check_variable = { chaos > 6 }
			}

			modifier = {
				factor = 10
				check_variable = { chaos > 9 }
			}
		}
	}

	ROM_declare_martial_law = {

		icon = generic_army_support

		available = {
			custom_trigger_tooltip = {
				tooltip = ROM_declare_martial_law_tooltip
				OR = {
					has_idea = ROM_chaos_high
					has_idea = ROM_anarchy
				}
			}
		}

		fire_only_once = yes

		cost = 100


		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_declare_martial_law"
			ROM_decrease_chaos_by_4 = yes
		}

		ai_will_do = {
			factor = 1

			modifier = {
				factor = 100
				has_idea = ROM_anarchy
			}
		}
	}

	ROM_end_white_terror = {

		icon = generic_prepare_civil_war

		fire_only_once = yes

		cost = 50

		days_remove = 14

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ROM_end_white_terror"
			ROM_decrease_chaos = yes
		}

		ai_will_do = {
			factor = 100
		}
	}
}
