 ############################
 # Nepal Decisions          #
 ############################
 # By Cazadorian & El Daddy #
 ############################

NEP_the_darjeeling_conspiracy_category = {
	NEP_conspiracy_mission = {
		icon = generic_political_discourse

		allowed = { always = no }
		priority = 1000

		available = {
			hidden_trigger = { always = no }
		}

		is_good = yes
		fire_only_once = yes
		days_mission_timeout = 300

		timeout_effect = {
			log = "[GetLogRoot]: Decision timeout NEP_conspiracy_mission"
			if = {
				limit = {
					NEP = { NEP_BHC_influence_winning = yes }
				}
				effect_tooltip = {
					NEP = { country_event = nepal_events.18 }
				}
			}
			else_if = {
				limit = {
					NEP = { NEP_DEH_influence_winning = yes }
				}
				effect_tooltip = {
					NEP = { country_event = nepal_events.25 }
				}
			}
			else_if = {
				limit = {
					NEP = { NEP_NEP_influence_winning = yes }
				}
				effect_tooltip = {
					NEP = { country_event = nepal_events.22 }
				}
			}
			hidden_effect = {
				if = {
					limit = { tag = NEP }
					if = {
						limit = {
							OR = {
								has_country_flag = NEP_conspiracy_BHC_ai
								AND = {
									NEP_conspiracy_ai_trigger = no
									NEP_BHC_influence_winning = yes
								}
							}
						}
						country_event = nepal_events.18
						DEH = { country_event = nepal_events.20 }
						BHC = { country_event = nepal_events.20 }
					}
					else_if = {
						limit = {
							OR = {
								has_country_flag = NEP_conspiracy_NEP_ai
								AND = {
									NEP_conspiracy_ai_trigger = no
									NEP_NEP_influence_winning = yes
								}
							}
						}
						country_event = nepal_events.22
					}
					else_if = {
						limit = {
							OR = {
								has_country_flag = NEP_conspiracy_DEH_ai
								AND = {
									NEP_conspiracy_ai_trigger = no
									NEP_DEH_influence_winning = yes
								}
							}
						}
						country_event = nepal_events.25
						DEH = { country_event = nepal_events.26 }
						BHC = { country_event = nepal_events.26 }
					}
					clr_country_flag = NEP_conspiracy_ongoing_flag
					remove_ideas = {
						NEP_crushing_the_conspirators_1
						NEP_crushing_the_conspirators_2
						NEP_covert_support_for_the_mukti_sena_1
						NEP_covert_support_for_the_mukti_sena_2_socialist
						NEP_covert_support_for_the_mukti_sena_2_democratic
						NEP_crushing_the_conspirators_monarchy
						NEP_covert_support_for_the_mukti_sena_monarchy
					}
				}
			}
		}
		fixed_random_seed = no
	}
	NEP_weaken_BHC_influence_decision = {
		icon = GFX_decision_generic_protection

		allowed = { always = no }	# Targeted decision activated by event
		priority = 200

		available = {
			check_variable = { NEP.NEP_conspiracy_influence_var_@FROM > 5 }
		}

		visible = {
			country_exists = BHC
		}

		cancel_trigger = {
			OR = {
				NEP_conspiracy_ongoing = no
				NOT = { country_exists = BHC }
			}
		}

		target_trigger = {
			NEP_conspiracy_ongoing = yes
		}

		cost = NEP.NEP_conspiracy_lower_cost_@BHC
		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = NEP_conspiracy_ai_flag
			}
			modifier = {
				factor = 0
				NEP_increase_BHC_trigger = yes
			}
			modifier = {
				factor = 0
				NEP_conspiracy_near_end = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_weaken_BHC_influence_decision"
			add_political_power = var:NEP.NEP_conspiracy_lower_cost_@BHC
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_weaken_BHC_influence_decision"
			NEP = { set_temp_variable = { NEP_conspiracy_influence_change = -5 } }
			NEP_change_influence_BHC = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_weaken_BHC_influence_decision"
			NEP_conspiracy_rng_effect = yes
		}
	}
	NEP_weaken_DEH_influence_decision = {
		icon = GFX_decision_SWI_dismiss_council

		allowed = { always = no }	# Targeted decision activated by event
		priority = 200

		available = {
			check_variable = { NEP.NEP_conspiracy_influence_var_@FROM > 5 }
		}

		visible = {
			country_exists = DEH
		}

		cancel_trigger = {
			OR = {
				NEP_conspiracy_ongoing = no
				NOT = { country_exists = DEH }
			}
		}

		target_trigger = {
			NEP_conspiracy_ongoing = yes
		}

		cost = NEP.NEP_conspiracy_lower_cost_@DEH
		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = NEP_conspiracy_ai_flag
			}
			modifier = {
				factor = 0
				NEP_increase_DEH_trigger = yes
			}
			modifier = {
				factor = 0
				NEP_conspiracy_near_end = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_weaken_DEH_influence_decision"
			add_political_power = var:NEP.NEP_conspiracy_lower_cost_@DEH
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_weaken_DEH_influence_decision"
			NEP = { set_temp_variable = { NEP_conspiracy_influence_change = -5 } }
			NEP_change_influence_DEH = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_weaken_DEH_influence_decision"
			NEP_conspiracy_rng_effect = yes
		}
	}
	NEP_increase_BHC_influence_decision = {
		icon = eng_trade_unions_support
		priority = 250

		allowed = { always = no }	# Targeted decision activated by event

		visible = {
			country_exists = BHC
		}

		cancel_trigger = {
			OR = {
				NEP_conspiracy_ongoing = no
				NOT = { country_exists = BHC }
			}
		}

		target_trigger = {
			NEP_conspiracy_ongoing = yes
		}

		cost = NEP.NEP_conspiracy_increase_cost_@BHC
		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = NEP_conspiracy_ai_flag
			}
			modifier = {
				factor = 0
				tag = NEP
				NEP_increase_DEH_trigger = yes
			}
			modifier = {
				factor = 0
				NEP_conspiracy_near_end = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_increase_BHC_influence_decision"
			add_political_power = var:NEP.NEP_conspiracy_increase_cost_@BHC
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_increase_BHC_influence_decision"
			NEP = { set_temp_variable = { NEP_conspiracy_influence_change = 5 } }
			NEP_change_influence_BHC = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_increase_BHC_influence_decision"
			NEP_conspiracy_rng_effect = yes
		}
	}
	NEP_increase_DEH_influence_decision = {
		icon = eng_trade_unions_support
		priority = 250

		allowed = { always = no }	# Targeted decision activated by event

		visible = {
			country_exists = DEH
		}

		cancel_trigger = {
			OR = {
				NEP_conspiracy_ongoing = no
				NOT = { country_exists = DEH }
			}
		}

		target_trigger = {
			NEP_conspiracy_ongoing = yes
		}

		cost = NEP.NEP_conspiracy_increase_cost_@DEH
		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = NEP_conspiracy_ai_flag
			}
			modifier = {
				factor = 0
				tag = NEP
				NEP_increase_BHC_trigger = yes
			}
			modifier = {
				factor = 0
				NEP_conspiracy_near_end = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_increase_DEH_influence_decision"
			add_political_power = var:NEP.NEP_conspiracy_increase_cost_@DEH
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_increase_DEH_influence_decision"
			NEP = { set_temp_variable = { NEP_conspiracy_influence_change = 5 } }
			NEP_change_influence_DEH = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_increase_DEH_influence_decision"
			NEP_conspiracy_rng_effect = yes
		}
	}
	NEP_india_send_guns_decision = {
		icon = GFX_decision_generic_civil_support
		priority = 200

		allowed = { always = no }	# Targeted decision activated by event

		visible = {
			country_exists = NEP
		}

		cancel_trigger = {
			OR = {
				NEP = { NEP_conspiracy_ongoing = no }
				NOT = { country_exists = NEP }
			}
		}

		target_trigger = {
			NEP = { NEP_conspiracy_ongoing = yes }
		}

		custom_cost_text = 150_guns
		custom_cost_trigger = {
			NOT = { has_equipment = { infantry_equipment < 150 } }
		}

		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = NEP_conspiracy_ai_flag
			}
			modifier = {
				factor = 0
				tag = DEH
				NEP_DEH_influence_winning_extra = yes
			}
			modifier = {
				factor = 0
				tag = BHC
				NEP_BHC_influence_winning_extra = yes
			}
			modifier = {
				factor = 0
				NEP_conspiracy_near_end = yes
			}
			modifier = {
				factor = 10
				tag = DEH
				NEP_BHC_influence_winning_extra = yes
			}
			modifier = {
				factor = 10
				tag = BHC
				NEP_DEH_influence_winning_extra = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_india_send_guns_decision"
			add_equipment_to_stockpile = {
				type = infantry_equipment
				amount = 150
			}
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_india_send_guns_decision"
			NEP = { set_temp_variable = { NEP_conspiracy_influence_change = 5 } }
			NEP_change_influence_ROOT = yes
			hidden_effect = {
				add_equipment_to_stockpile = {
					type = infantry_equipment
					amount = -150
				}
			}
			NEP_conspiracy_rng_effect = yes
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_india_send_guns_decision"
		}
	}
	NEP_india_command_power_decision = {
		icon = GFX_decision_generic_assassination
		priority = 200

		allowed = { always = no }	# Targeted decision activated by event

		visible = {
			country_exists = NEP
		}

		cancel_trigger = {
			OR = {
				NEP = { NEP_conspiracy_ongoing = no }
				NOT = { country_exists = NEP }
			}
		}

		target_trigger = {
			NEP = { NEP_conspiracy_ongoing = yes }
		}

		custom_cost_text = CP_25
		custom_cost_trigger = {
			NOT = { command_power < 25 }
		}

		days_remove = 15
		fire_only_once = no
		fixed_random_seed = no

		ai_will_do = {
			factor = 100
			modifier = {
				factor = 0
				has_country_flag = NEP_conspiracy_ai_flag
			}
			modifier = {
				factor = 0
				tag = DEH
				NEP_DEH_influence_winning_extra = yes
			}
			modifier = {
				factor = 0
				tag = BHC
				NEP_BHC_influence_winning_extra = yes
			}
			modifier = {
				factor = 0
				NEP_conspiracy_near_end = yes
			}
			modifier = {
				factor = 10
				tag = DEH
				NEP_BHC_influence_winning_extra = yes
			}
			modifier = {
				factor = 10
				tag = BHC
				NEP_DEH_influence_winning_extra = yes
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_india_command_power_decision"
			add_command_power = 25
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_india_command_power_decision"
			NEP = { set_temp_variable = { NEP_conspiracy_influence_change = 5 } }
			NEP_change_influence_ROOT = yes
			hidden_effect = { add_command_power = -25 }
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_india_command_power_decision"
			NEP_conspiracy_rng_effect = yes
		}
	}
}

foreign_politics = {
	NEP_join_faction = {
		name = NEP_join_faction_name
		icon = GFX_decision_hol_exchange_intelligence_data

		allowed = { always = no } #Manually activated

		available = {
			at_peace_or_same_enemies_as_FROM = yes
			ROOT = {
				OR = {
					any_home_area_neighbor_country = { is_ally_with = FROM }
					any_subject_country = { any_home_area_neighbor_country = { is_ally_with = FROM } }
				}
			}
			FROM = {
				is_in_faction = yes
				NOT = { is_in_faction_with = DEH }
			}
		}

		visible = {
			is_subject = no
			is_in_faction = no
			NOT = { has_war_with = FROM }
			NOT = { has_country_flag = NEP_joining_faction }
		}

		target_trigger = {
			is_subject = no
			FROM = {
				has_socialist_government = no
				NOT = { has_war_with = ROOT }
			}
		}

		cost = 30
		fire_only_once = yes

		ai_will_do = {
			factor = 2000
			modifier = {
				factor = 0
				FROM = { surrender_progress > 0.3 }
				has_game_rule = { rule = NEP_forpol option = DEFAULT }
			}
			modifier = {
				factor = 0
				OR = {
					AND = {
						FROM = { tag = PRF }
						has_game_rule = { rule = NEP_forpol option = NEP_FORPOL_JAP }
					}
					AND = {
						FROM = { tag = JAP }
						has_game_rule = { rule = NEP_forpol option = NEP_FORPOL_PRF }
					}
				}
			}
		}

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_join_faction"
			set_country_flag = NEP_joining_faction
			FROM = { country_event = nepal.100 }
		}
		fixed_random_seed = no
	}
	NEP_attack_bhutan_decision = {
		icon = generic_prepare_civil_war
		allowed = { tag = NEP }

		available = {
			is_neighbor_of = FROM
			NOT = { has_non_aggression_pact_with = FROM }
			NOT = { has_guaranteed = FROM }
		}

		visible = {
			has_completed_focus = NEP_resolving_the_bhutan_conflict
			can_declare_war_on = FROM
			NOT = { has_wargoal_against = FROM }
			NOT = { is_justifying_wargoal_against = FROM }
			FROM = { is_in_same_economic_sphere_as_ROOT = no }
		}

		cancel_trigger = {
			OR = {
				NOT = { can_declare_war_on = FROM }
				has_wargoal_against = FROM
				is_justifying_wargoal_against = FROM
				FROM = { is_in_same_economic_sphere_as_ROOT = yes }
				NOT = { is_neighbor_of = FROM }
				has_non_aggression_pact_with = FROM
				has_guaranteed = FROM
			}
		}

		targets = { 324.OWNER }
		target_root_trigger = { has_completed_focus = NEP_resolving_the_bhutan_conflict }
		target_trigger = {
			has_completed_focus = NEP_resolving_the_bhutan_conflict
			can_declare_war_on = FROM
			NOT = { has_wargoal_against = FROM }
			NOT = { is_justifying_wargoal_against = FROM }
			FROM = { is_in_same_economic_sphere_as_ROOT = no }
		}

		cost = 25
		days_remove = 30
		war_with_target_on_remove = yes

		ai_will_do = {
			base = 0 #People doing observer games get mad if Bhutan dies
			modifier = {
				add = 10
				BHU = { is_ai = no }
			}
			modifier = {
				factor = 0
				OR = {
					country_is_in_dangerous_war = yes
					FROM = { default_attack_AI_trigger = yes }
					FROM = { is_in_faction = yes }
					FROM = { is_subject = yes }
				}
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_attack_bhutan_decision"
			clear_decision_attack_AI = yes
		}
		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_attack_bhutan_decision"
			setup_decision_attack_AI = yes
			FROM = { incoming_war_notification_effect = yes }
		}
		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_attack_bhutan_decision"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
		fixed_random_seed = no
	}
	NEP_attack_tibet_decision = {
		icon = generic_prepare_civil_war
		allowed = { tag = NEP }

		available = {
			is_neighbor_of = FROM
			NOT = { has_non_aggression_pact_with = FROM }
			NOT = { has_guaranteed = FROM }
		}

		visible = {
			has_completed_focus = NEP_punishing_tibetan_insolence
			can_declare_war_on = FROM
			NOT = { has_wargoal_against = FROM }
			NOT = { is_justifying_wargoal_against = FROM }
			FROM = { is_in_same_economic_sphere_as_ROOT = no }
		}

		cancel_trigger = {
			OR = {
				NOT = { can_declare_war_on = FROM }
				has_wargoal_against = FROM
				is_justifying_wargoal_against = FROM
				FROM = { is_in_same_economic_sphere_as_ROOT = yes }
				NOT = { is_neighbor_of = FROM }
				has_non_aggression_pact_with = FROM
				has_guaranteed = FROM
			}
		}

		targets = { 322.OWNER }
		target_root_trigger = { has_completed_focus = NEP_punishing_tibetan_insolence }
		target_trigger = {
			has_completed_focus = NEP_punishing_tibetan_insolence
			can_declare_war_on = FROM
			NOT = { has_wargoal_against = FROM }
			NOT = { is_justifying_wargoal_against = FROM }
			FROM = { is_in_same_economic_sphere_as_ROOT = no }
		}

		cost = 25
		days_remove = 30
		war_with_target_on_remove = yes

		ai_will_do = {
			factor = 10
			modifier = {
				factor = 0
				OR = {
					country_is_in_dangerous_war = yes
					FROM = { default_attack_AI_trigger = yes }
					FROM = { is_in_faction = yes }
					FROM = { is_subject = yes }
				}
			}
		}

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_attack_tibet_decision"
			clear_decision_attack_AI = yes
		}
		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_attack_tibet_decision"
			setup_decision_attack_AI = yes
			FROM = { incoming_war_notification_effect = yes }
		}
		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_attack_tibet_decision"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = annex_everything
			}
		}
		fixed_random_seed = no
	}

	NEP_follow_the_calcutta_line = {
		icon = GFX_decision_soldierly_discipline
		allowed = { always = no }
		available = {
			has_socialist_government = yes
			BHC = {
				has_completed_focus = BHC_first_united_congress
				is_subject = no
			}
			if = {
				limit = { BHC = { has_government = radical_socialist } }
				NOT = { has_government = radical_socialist }
			}
			else_if = {
				limit = { BHC = { has_government = syndicalist } }
				NOT = { has_government = syndicalist }
			}
			else_if = {
				limit = { BHC = { has_government = totalist } }
				NOT = { has_government = totalist }
			}
		}
		visible = {
			BHC = {
				exists = yes
				has_socialist_government = yes
			}
		}

		cost = 20

		ai_will_do = { factor = 100 }

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_follow_the_calcutta_line"
			if = {
				limit = { BHC = { has_government = radical_socialist } }
				custom_effect_tooltip = NEP_follow_the_calcutta_line_radical_socialist_tt
				set_politics = {
					ruling_party = radical_socialist
					elections_allowed = no
				}
				set_temp_variable = { coalition_partner_var = token:totalist }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:syndicalist }
				add_to_coalition = yes
			}
			else_if = {
				limit = { BHC = { has_government = syndicalist } }
				custom_effect_tooltip = NEP_follow_the_calcutta_line_syndicalist_tt
				set_politics = {
					ruling_party = syndicalist
					elections_allowed = no
				}
				set_temp_variable = { coalition_partner_var = token:totalist }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:radical_socialist }
				add_to_coalition = yes
			}
			else_if = {
				limit = { BHC = { has_government = totalist } }
				custom_effect_tooltip = NEP_follow_the_calcutta_line_totalist_tt
				set_politics = {
					ruling_party = totalist
					elections_allowed = no
				}
				set_temp_variable = { coalition_partner_var = token:radical_socialist }
				add_to_coalition = yes
				set_temp_variable = { coalition_partner_var = token:syndicalist }
				add_to_coalition = yes
			}
			add_popularity = { ideology = FROM popularity = 0.20 }
		}
		fixed_random_seed = no
	}
}

NEP_surviving_the_indian_nightmare_category = {
	NEP_pilfer_the_depots = {
		icon = GFX_decision_generic_prepare_civil_war
		allowed = { always = no } #Activated Manually

		cost = 50
		fire_only_once = yes
		ai_will_do = { factor = 10 }

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_pilfer_the_depots"
			add_equipment_to_stockpile = {
				type = infantry_equipment_0
				amount = 1000
				producer = GBR
			}
			hidden_effect = {
				remove_targeted_decision = { decision = NEP_pilfer_the_depots target = FROM }
			}
		}
		fixed_random_seed = no
	}

	NEP_emergency_military_production = {
		icon = GFX_decision_ger_military_buildup
		allowed = { always = no } #Activated Manually
		available = { has_war = yes }

		cost = 60
		fire_only_once = yes
		days_remove = 100
		ai_will_do = { factor = 10 }

		modifier = {
			production_speed_buildings_factor = -0.4
			industrial_capacity_factory = 0.15
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_emergency_military_production"
			hidden_effect = {
				remove_targeted_decision = { decision = NEP_emergency_military_production target = FROM }
			}
		}
		fixed_random_seed = no
	}

	NEP_fortify_uttaranchal = {
		icon = GFX_decision_generic_mountain_fortification

		allowed = { always = no } #Activated Manually

		available = {
			FROM = { is_fully_controlled_by = owner }
			OR = {
				has_war_with = DEH
				has_war = no
			}
		}

		visible = {
			OR = {
				owns_state = FROM
				AND = {
					is_subject = yes
					overlord = { owns_state = FROM }
				}
			}
		}

		cancel_trigger = {
			NOT = { FROM = { is_fully_controlled_by = owner } }
		}

		state_target = yes
		target_trigger = {
			OR = {
				owns_state = FROM
				AND = {
					is_subject = yes
					overlord = { owns_state = FROM }
				}
			}
		}

		cost = 40

		ai_will_do = { factor = 10 }

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_fortify_uttaranchal"
			add_political_power = 40
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_fortify_uttaranchal"
			FROM = {
				custom_override_tooltip = {
					add_building_construction = {
						type = bunker
						province = 5157
						level = 2
						instant_build = yes
					}
					tooltip = {
						localization_key = tooltip_add_building_on_victory_point
						BUILDING = bunker
						NUMBER = 2
						VP = 5157
					}
				}
				add_building_construction = {
					type = bunker
					province = 3989
					level = 2
					instant_build = yes
				}
				add_building_construction = {
					type = bunker
					province = 5002
					level = 2
					instant_build = yes
				}
			}
			hidden_effect = {
				remove_targeted_decision = { decision = NEP_fortify_uttaranchal target = FROM }
			}
		}
		fixed_random_seed = no
	}

	NEP_fortify_darjeeling = {
		icon = GFX_decision_fortress

		allowed = { always = no } #Activated Manually

		available = {
			FROM = { is_fully_controlled_by = owner }
			OR = {
				has_war_with = BHC
				has_war = no
			}
		}

		visible = {
			OR = {
				owns_state = FROM
				AND = {
					is_subject = yes
					overlord = { owns_state = FROM }
				}
			}
		}

		cancel_trigger = {
			NOT = { FROM = { is_fully_controlled_by = owner } }
		}

		state_target = yes
		target_trigger = {
			OR = {
				owns_state = FROM
				AND = {
					is_subject = yes
					overlord = { owns_state = FROM }
				}
			}
		}

		cost = 40

		ai_will_do = { factor = 10 }

		cancel_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision cancel NEP_fortify_darjeeling"
			add_political_power = 40
		}

		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_fortify_darjeeling"
			FROM = {
				custom_override_tooltip = {
					add_building_construction = {
						type = bunker
						province = {
							all_provinces = yes
							limit_to_border = yes
						}
						level = 2
						instant_build = yes
					}
					tooltip = {
						localization_key = tooltip_add_building_on_border_provinces
						BUILDING = bunker
						NUMBER = 2
					}
				}
			}
			hidden_effect = {
				remove_targeted_decision = { decision = NEP_fortify_darjeeling target = FROM }
			}
		}
		fixed_random_seed = no
	}

	NEP_retreat_to_kathmandu = {
		icon = GFX_decision_infiltrate_state
		allowed = { always = no } #Activated Manually
		available = {
			surrender_progress > 0.05
			has_war = yes
		}

		fire_only_once = yes
		ai_will_do = { factor = 10 }
		cancel_if_not_visible = no

		custom_cost_trigger = {
			NOT = { command_power < 25 }
		}
		custom_cost_text = CP_25
		modifier = {
			army_core_defence_factor = 0.1
			dig_in_speed_factor = 0.25
		}
		days_remove = 60

		complete_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision complete NEP_retreat_to_kathmandu"
			hidden_effect = { add_command_power = -25 }
		}
		remove_effect = {
			log = "[GetLogRoot][GetLogFrom]: Decision remove NEP_retreat_to_kathmandu"
			hidden_effect = {
				remove_targeted_decision = { decision = NEP_retreat_to_kathmandu target = FROM }
			}
		}

		fixed_random_seed = no
	}
}

political_actions = {
	NEP_assert_the_mantle_of_asal_hindustan = {
		icon = generic_form_nation
		fire_only_once = yes
		allowed = { tag = NEP }
		visible = {
			owns_state = 439
			owns_state = 431
			owns_state = 427
		}
		available = {
			owns_state = 439
			owns_state = 431
			owns_state = 427
			NOT = {
				country_exists = DEH
				country_exists = PRF
				country_exists = BHC
				country_exists = PGI
			}
		}
		fire_only_once = yes
		cost = 100

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete NEP_assert_the_mantle_of_asal_hindustan target: [From.GetName]"
			every_state = {
				limit = { is_on_continent = india }
				add_core_of = NEP
			}
			
			set_cosmetic_tag = NEP_HINDUSTAN
			
			if = {
				limit = { country_exists = BHU }
				create_wargoal = { type = annex_everything target = BHU }
			}
			if = {
				limit = { country_exists = SKM }
				create_wargoal = { type = annex_everything target = SKM }
			}
			if = {
				limit = { country_exists = MAD }
				create_wargoal = { type = annex_everything target = MAD }
			}
		}
	}
}