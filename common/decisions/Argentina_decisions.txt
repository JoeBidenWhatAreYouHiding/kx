###########################
# Argentina Decisions
###########################
# By Jeankdezeehond
###########################

ARG_shared_industry_decisions = {
	ARG_Vina_Dam_mission = {
		allowed = { always = no }	# Activated by argbla.11
		icon = GFX_decision_cement_mixer
		available = {
			hidden_trigger = { always = no }
		}

		selectable_mission = yes
		days_mission_timeout = 1440
		is_good = yes
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Vina_Dam_mission"
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout ARG_Vina_Dam_mission"
			902 = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
		}

		ai_will_do = { factor = 200 }
	}

	ARG_La_Mesopotamia_Rail = {					# Cover the supply in the north vs PAR
		icon = GFX_decision_train
		allowed = { always = no }
		available = {
			has_tech = construction2
			num_of_civilian_factories_available_for_projects > 0
			if = {
				limit = { tag = FOP }
				FOP_has_economic_fallout = no
			}
		}

		modifier = {
			political_power_gain = -0.1
			civilian_factory_use = 1
		}

		fire_only_once = yes
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_La_Mesopotamia_Rail"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_La_Mesopotamia_Rail"
			build_railway = { start_province = 12364 target_province = 12964 level = 2 }
			build_railway = { start_province = 12364 target_province = 11007 level = 2 }
			set_country_flag = ARG_La_Mesopotamia_Rail_finished
		}

		ai_will_do = { base = 50 }
	}

	ARG_Neuquen_Rail = {						# Connect capital and Patagonia with Nequen
		icon = GFX_decision_train
		allowed = { always = no }				# Activated by focus ARG_Parana_Infrastructure_Plan
		available = {
			has_tech = construction2
			num_of_civilian_factories_available_for_projects > 0
			if = {
				limit = { tag = FOP }
				FOP_has_economic_fallout = no
			}
		}

		modifier = {
			political_power_gain = -0.1
			civilian_factory_use = 1
		}

		fire_only_once = yes
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Neuquen_Rail"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Neuquen_Rail"
			build_railway = { start_province = 12364 target_province = 12306 level = 2 }
			build_railway = {
				path = { 12306 13226 13305 }
				level = 2
			}
			set_country_flag = ARG_Neuquen_Rail_finished
		}

		ai_will_do = { base = 50 }
	}

	ARG_jujuy_Rail = {							# Cover the supply in the north vs CHL and BOL
		icon = GFX_decision_train
		allowed = { always = no }				# Activated by focus ARG_Parana_Infrastructure_Plan
		available = {
			has_tech = construction2
			num_of_civilian_factories_available_for_projects > 0
			if = {
				limit = { tag = FOP }
				FOP_has_economic_fallout = no
			}
		}

		modifier = {
			political_power_gain = -0.1
			civilian_factory_use = 1
		}

		fire_only_once = yes
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_jujuy_Rail"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_jujuy_Rail"
			build_railway = { start_province = 12364 target_province = 11001 level = 2 }
			set_country_flag = ARG_jujuy_rail_finished
		}

		ai_will_do = { base = 50 }
	}

	ARG_Mendoza_Rail = {						# Cover the supply in the west vs CHL
		icon = GFX_decision_train
		allowed = { always = no }				# Activated by focus ARG_Parana_Infrastructure_Plan
		available = {
			has_tech = construction2
			num_of_civilian_factories_available_for_projects > 0
			if = {
				limit = { tag = FOP }
				FOP_has_economic_fallout = no
			}
		}

		modifier = {
			political_power_gain = -0.1
			civilian_factory_use = 1
		}

		fire_only_once = yes
		days_remove = 180

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Mendoza_Rail"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Mendoza_Rail"
			build_railway = { start_province = 12364 target_province = 8215 level = 2 }
			set_country_flag = ARG_Mendoza_Rail_finished
		}

		ai_will_do = { base = 50 }
	}
## Dockyards
	ARG_dockyards_dec = {
		icon = GFX_decision_generic_merge_plant_ship
		allowed = { always = no }
		cost = 30

		available = {
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 5 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_dockyards_dec target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = dockyard
					level = 1
					instant_build = yes
				}
			}
			activate_targeted_decision = { target = FROM decision = ARG_more_dockyards_dec }
		}
	}
	ARG_more_dockyards_dec = {
		icon = GFX_decision_generic_merge_plant_ship
		allowed = { always = no }
		cost = 50

		available = {
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 150
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 5 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_more_dockyards_dec target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = dockyard
					level = 2
					instant_build = yes
				}
			}
		}
	}
	ARG_arms_industry_investments = {
		icon = GFX_decision_generic_industry
		allowed = { always = no }
		cost = 50

		available = {
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_arms_industry_investments target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_arms_industry_investments target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = arms_factory
					level = 1
					instant_build = yes
				}
			}
			activate_targeted_decision = { target = FROM decision = ARG_more_arms_industry_investments }
		}
	}
	ARG_more_arms_industry_investments = {
		icon = GFX_decision_generic_industry
		allowed = { always = no }
		cost = 50

		available = {
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 150
		fire_only_once = yes
		state_target = yes

		ai_will_do = { factor = 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_more_arms_industry_investments target: [From.GetName]"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_more_arms_industry_investments target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = arms_factory
					level = 2
					instant_build = yes
				}
			}
		}
	}
## Civ factories
	ARG_improve_wheat_farms_dec = {
		icon = GFX_decision_generic_agriculture
		allowed = { always = no }
		cost = 50

		available = {
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 150
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 40 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_improve_wheat_farms_dec target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 2
				add_building_construction = {
					type = industrial_complex
					level = 2
					instant_build = yes
				}
			}
		}
	}
	ARG_expand_cotton_production_dec = {
		icon = GFX_decision_cotton
		allowed = { always = no }
		cost = 40

		available = {
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 40 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_expand_cotton_production_dec target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
				if = {
					limit = { NOT = { is_core_of = ROOT } }
					add_compliance = 5
					add_resistance = -5
				}
			}
		}
	}
	ARG_expand_forestry_dec = {
		icon = GFX_decision_forestry
		allowed = { always = no }
		cost = 40

		available = {
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 40 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_expand_forestry_dec target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
				if = {
					limit = { NOT = { is_core_of = ROOT } }
					add_compliance = 5
					add_resistance = -5
				}
			}
		}
	}
	ARG_expand_fishing_dec = {
		icon = GFX_decision_cod
		allowed = { always = no }
		cost = 20

		available = {
			has_equipment = { convoy > 9 }
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 40 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_expand_fishing_dec target: [From.GetName]"
			add_equipment_to_stockpile = { type = convoy amount = -10 }
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_expand_fishing_dec target: [From.GetName]"
			FROM = {
				add_extra_state_shared_building_slots = 1
				add_building_construction = {
					type = industrial_complex
					level = 1
					instant_build = yes
				}
			}
		}
	}
## State category
	ARG_expand_animal_husbandry_dec = {
		name = [ARG_expand_animal_husbandry_scripted_loc]
		icon = GFX_decision_animal_husbandry
		allowed = { always = no }
		cost = 20

		available = {
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
			if = {
				limit = { tag = FOP }
				FOP_has_economic_fallout = no
			}
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 100
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_expand_animal_husbandry_dec target: [From.GetName]"
			FROM = {
				increase_state_category_by_one_level = yes
			}
		}
	}
## Infrastructure
	ARG_expand_local_infrastructure_tier1 = {
		icon = GFX_decision_road
		allowed = { always = no }
		cost = 20

		available = {
			num_of_civilian_factories_available_for_projects > 1
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 2
		}

		days_remove = 100
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 2 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_expand_local_infrastructure_tier1 target: [From.GetName]"
			FROM = {
				if = {
					limit = { infrastructure < 5 }
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
				else = {
					add_extra_state_shared_building_slots = 1
				}
			}
		}
	}
## Oil extraction decisions - Patagonia
	ARG_YPF_expansion_dec = {
		name = [ARG_YPF_expansion_scripted_loc]
		icon = GFX_decision_oil
		allowed = { always = no }
		cost = 30

		available = {
			has_tech = excavation3
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 30 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_YPF_expansion_dec target: [From.GetName]"
			FROM = {
				add_resource = {
					type = oil
					amount = 5
				}
			}
		}
	}
	ARG_YPF_expansion_limited_dec = {
		name = [ARG_YPF_expansion_scripted_loc]
		icon = GFX_decision_oil
		allowed = { always = no }
		cost = 30

		available = {
			has_tech = excavation2
			num_of_civilian_factories_available_for_projects > 2
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 3
		}

		days_remove = 120
		fire_only_once = yes
		state_target = yes

		ai_will_do = { base = 30 }

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_YPF_expansion_limited_dec target: [From.GetName]"
			FROM = {
				add_resource = {
					type = oil
					amount = 3
				}
			}
		}
	}
## Oil extraction - Add Mesopotamia (510), Nequen (579) and Bolivian Chaco (1027) here
## Tungsten mining - TODO - state 848 (BOL), 302 (BOL)
	ARG_Santiago_del_Estero_preliminary_excavation_studies = {
		icon = generic_operation
		allowed = { always = no }
		fire_only_once = yes

		cost = 20

		days_remove = 90

		modifier = {
			research_speed_factor = -0.05
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Santiago_del_Estero_preliminary_excavation_studies"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Santiago_del_Estero_preliminary_excavation_studies"
			activate_targeted_decision = { target = 509 decision = ARG_local_tungsten_extraction_in_Santiago_del_Estero }
			activate_targeted_decision = { target = 509 decision = ARG_expand_local_infrastructure_tier1 }
		}

		ai_will_do = { base = 25 }
	}

	ARG_local_tungsten_extraction_in_Santiago_del_Estero = {
		icon = GFX_decision_tungsten
		allowed = { always = no }				# Activated by previous decision
		available = {
			has_tech = excavation2
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		fire_only_once = yes
		state_target = yes
		cost = 30
		days_remove = 120

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_local_tungsten_extraction_in_Santiago_del_Estero target: [From.GetName]"
			FROM = {
				add_resource = {
					type = tungsten
					amount = 10
				}
				add_resource = {
					type = steel
					amount = 3
				}
				add_building_construction = {
					type = supply_node
					province = 5143
					level = 1
					instant_build = yes
				}
			}
		}

		ai_will_do = { base = 25 }
	}
	### Tungsten in BOL if it is occupied/ally
	ARG_PRU_preliminary_excavation_studies_1 = {
		icon = generic_operation
		allowed = { always = no }
		fire_only_once = yes

		cost = 20

		days_remove = 90

		modifier = {
			research_speed_factor = -0.05
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_PRU_preliminary_excavation_studies_1"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_PRU_preliminary_excavation_studies_1"
			activate_targeted_decision = { target = 848 decision = ARG_PRU_local_tungsten_extraction_1 }
			activate_targeted_decision = { target = 848 decision = ARG_expand_local_infrastructure_tier1 }
		}

		ai_will_do = { base = 25 }
	}
	ARG_PRU_local_tungsten_extraction_1 = {
		name = ARG_PRU_local_tungsten_extraction
		icon = GFX_decision_tungsten
		allowed = { always = no }				# Activated by previous decision
		available = {
			has_tech = excavation2
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		fire_only_once = yes
		state_target = yes
		cost = 30
		days_remove = 120

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_PRU_local_tungsten_extraction_1 target: [From.GetName]"
			FROM = {
				add_resource = {
					type = tungsten
					amount = 10
				}
				add_resource = {
					type = steel
					amount = 3
				}
			}
		}

		ai_will_do = { base = 25 }
	}
	ARG_PRU_preliminary_excavation_studies_2 = {
		icon = generic_operation
		allowed = { always = no }
		fire_only_once = yes

		cost = 20

		days_remove = 90

		modifier = {
			research_speed_factor = -0.05
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_PRU_preliminary_excavation_studies_2"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_PRU_preliminary_excavation_studies_2"
			activate_targeted_decision = { target = 302 decision = ARG_PRU_local_tungsten_extraction_2 }
			activate_targeted_decision = { target = 302 decision = ARG_expand_local_infrastructure_tier1 }
		}

		ai_will_do = { base = 25 }
	}
	ARG_PRU_local_tungsten_extraction_2 = {
		name = ARG_PRU_local_tungsten_extraction
		icon = GFX_decision_tungsten
		allowed = { always = no }				# Activated by previous decision
		available = {
			has_tech = excavation2
			num_of_civilian_factories_available_for_projects > 3
			FROM = { is_controlled_by = owner }
		}
		visible = {
			FROM = { is_owned_by_ROOT_or_ally = yes }
		}

		modifier = {
			civilian_factory_use = 4
		}

		fire_only_once = yes
		state_target = yes
		cost = 30
		days_remove = 120

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_PRU_local_tungsten_extraction_2 target: [From.GetName]"
			FROM = {
				add_resource = {
					type = tungsten
					amount = 10
				}
			}
		}

		ai_will_do = { base = 25 }
	}
## Steel production
	ARG_Ancidar_preliminary_excavation_studies = {
		icon = generic_operation
		allowed = { always = no }
		fire_only_once = yes

		cost = 20

		days_remove = 90

		modifier = {
			research_speed_factor = -0.05
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Ancidar_preliminary_excavation_studies"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Ancidar_preliminary_excavation_studies"
			activate_targeted_decision = { target = THIS decision = ARG_Coal_Mining }
		}

		ai_will_do = { base = 25 }
	}

	ARG_Coal_Mining = {
		icon = generic_construction
		allowed = { always = no }				# Activated by previous decision
		fire_only_once = yes

		cost = 20

		days_remove = 90

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Coal_Mining"
			add_tech_bonus = {
				name = ARG_Coal_Mining
				bonus = 1
				uses = 1
				category = excavation_tech
			}
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Coal_Mining"
			activate_targeted_decision = { target = 902 decision = ARG_Found_Acindar }
			activate_targeted_decision = { target = 902 decision = ARG_expand_local_infrastructure_tier1 }
		}

		ai_will_do = { base = 25 }
	}

	ARG_Found_Acindar = {
		icon = GFX_decision_steel
		allowed = { always = no }				# Activated by previous decision
		available = {
			has_tech = excavation3
			num_of_civilian_factories_available_for_projects > 3
			controls_state = FROM
		}
		modifier = {
			civilian_factory_use = 4
		}

		fire_only_once = yes
		state_target = yes
		cost = 30
		days_remove = 120

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Found_Acindar target: [From.GetName]"
			FROM = {
				add_resource = {
					type = steel
					amount = 12
				}
			}
		}

		ai_will_do = { base = 25 }
	}
	ARG_Patagonia_preliminary_excavation_studies = {
		icon = generic_operation
		allowed = { always = no }
		fire_only_once = yes

		cost = 20

		days_remove = 90

		modifier = {
			research_speed_factor = -0.05
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Patagonia_preliminary_excavation_studies"
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Patagonia_preliminary_excavation_studies"
			activate_targeted_decision = { target = 1029 decision = ARG_iron_mining_in_Patagonia }
		}

		ai_will_do = { base = 25 }
	}
	ARG_iron_mining_in_Patagonia = {
		icon = GFX_decision_steel
		allowed = { always = no }				# Activated by previous decision
		available = {
			has_tech = excavation2
			num_of_civilian_factories_available_for_projects > 2
			controls_state = FROM
		}
		modifier = {
			civilian_factory_use = 3
		}

		fire_only_once = yes
		state_target = yes
		cost = 30
		days_remove = 120

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_iron_mining_in_Patagonia target: [From.GetName]"
			FROM = {
				add_resource = {
					type = steel
					amount = 7
				}
			}
		}

		ai_will_do = { base = 25 }
	}
## Rubber production (BRA conq / investments) - state 855, 497
}

ARG_infrastructure_decisions = {
	ARG_Five_Year_mission = {

		allowed = {
			always = no
		}

		icon = ger_mefo_bills

		available = {
			custom_trigger_tooltip = {
				tooltip = ARG_has_black_monday_tt
				NOT = {
					ARG_has_black_monday = yes
				}
			}
			has_country_flag = ARG_La_Mesopotamia_Rail_finished
			has_country_flag = ARG_Neuquen_Rail_finished
			has_country_flag = ARG_jujuy_rail_finished
			has_country_flag = ARG_Mendoza_Rail_finished
			num_of_factories > 40
		}

		selectable_mission = no
		days_mission_timeout = 1825
		is_good = no
		fire_only_once = yes

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Five_Year_mission"
			custom_effect_tooltip = ARG_Succes_Five_Year_Plan
			add_political_power = 100
			add_stability = 0.05
		}

		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision timeout ARG_Five_Year_mission"
			custom_effect_tooltip = ARG_Failure_Five_Year_Plan
			add_political_power = -100
			add_stability = -0.1
		}
	}
}

ARG_immigration_decisions = {
	ARG_Attract_Austrian_Immigrants = {

		icon = generic_political_discourse

		targets = { AUS }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = AUS
			AUS = {
				OR = {
					has_completed_focus = AUS_status_quo
					has_completed_focus = AUS_military_occupation
				}
				NOT = {
					has_war_with = ARG
				}
			}
		}

		fire_only_once = no

		cost = 20

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Austrian_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 10 }
			custom_effect_tooltip = IMMI_AUS
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Belgian_Immigrants = {

		icon = generic_political_discourse

		targets = { BEL }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = BEL
			BEL = {
				NOT = { has_war_with = ARG }
				has_idea = BEL_anti_german_sentiment3
			}
		}

		fire_only_once = no

		cost = 10

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Belgian_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 5 }
			custom_effect_tooltip = IMMI_BEL
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Danish_Immigrants = {

		icon = generic_political_discourse

		targets = { DEN }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = DEN
			DEN = {
				NOT = { has_war_with = ARG }
				has_idea = DEN_idea_black_monday
			}
		}

		fire_only_once = no

		cost = 5

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Danish_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 2 }
			custom_effect_tooltip = IMMI_DEN
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Dutch_Immigrants = {

		icon = generic_political_discourse

		targets = { HOL }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = HOL
			HOL = {
				NOT = { has_war_with = ARG }
				OR = {
					has_socialist_government = yes
					has_authoritarian_government = yes
				}
			}
		}

		fire_only_once = no

		cost = 10

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Dutch_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 4 }
			custom_effect_tooltip = IMMI_HOL
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_German_Immigrants = {

		icon = generic_political_discourse

		targets = { GER }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			GER = {
				NOT = { has_war_with = ARG }
				OR = {
					exists = no
					has_war_with = FRA
					has_war_with = RUS
				}
			}
		}

		fire_only_once = no

		cost = 30

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_German_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 15 }
			custom_effect_tooltip = IMMI_GER
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Italian_Immigrants = {

		icon = generic_political_discourse

		targets = { ITA }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = ITA
			ITA = {
				NOT = { has_war_with = ARG }
			}
		}

		fire_only_once = no

		cost = 40

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Italian_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 20 }
			custom_effect_tooltip = IMMI_ITA
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Ottoman_Immigrants = {

		icon = generic_political_discourse

		targets = { TUR }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			TUR = {
				NOT = { has_war_with = ARG }
				OR = {
					exists = no
					has_completed_focus = OTT_Single_Party_Dictatorship
				}
			}
		}

		fire_only_once = no

		cost = 15

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Ottoman_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 8 }
			custom_effect_tooltip = IMMI_OTT
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Portugese_Immigrants = {

		icon = generic_political_discourse

		targets = { POR }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = POR
			POR = {
				NOT = { has_war_with = ARG }
				OR = {
					has_war = yes
					has_socialist_government = yes
				}
			}
		}

		fire_only_once = no

		cost = 5

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Portugese_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 2 }
			custom_effect_tooltip = IMMI_POR
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Russian_Immigrants = {

		icon = generic_political_discourse

		targets = { RUS }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = RUS
			RUS = {
				NOT = { has_war_with = ARG }
			}
		}

		fire_only_once = no

		cost = 20

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Russian_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 10 }
			custom_effect_tooltip = IMMI_RUS
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Spanish_Immigrants = {

		icon = generic_political_discourse

		targets = { SPR }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = SPR
			SPR = {
				NOT = {
					has_war_with = ARG
				}
			}
		}

		fire_only_once = no

		cost = 40

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Spanish_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 20 }
			custom_effect_tooltip = IMMI_SPR
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Attract_Swiss_Immigrants = {

		icon = generic_political_discourse

		targets = { SWI }

		target_trigger = {
			always = yes
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			has_stability > 0.4
			has_war = no
			custom_trigger_tooltip = {
				tooltip = arg_immiblack
				NOT = {
					has_idea = ARG_Black_Monday
				}
			}
		}

		visible = {
			country_exists = SWI
			SWI = {
				NOT = {
					has_war_with = ARG
				}
				OR = {
					has_socialist_government = yes
					has_authoritarian_government = yes
				}
			}
		}

		fire_only_once = no

		cost = 5

		days_re_enable = 365

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Attract_Swiss_Immigrants target: [From.GetName]"
			add_to_variable = { var = arg_im_factor value = 3 }
			custom_effect_tooltip = IMMI_SWI
		}

		ai_will_do = {
			factor = 5
		}
	}
}

ARG_autarky_decisions = {
	ARG_Universidad_Tecnologica_Nacional = {

		icon = generic_research

		allowed = {
			original_tag = ARG
		}

		fire_only_once = yes

		cost = 60

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Universidad_Tecnologica_Nacional"
			add_ideas = universidad_buenos_aires
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Preliminary_excavation_studies = {

		icon = generic_operation

		allowed = {
			original_tag = ARG
		}

		fire_only_once = yes

		cost = 20

		days_remove = 30

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Preliminary_excavation_studies"
			add_ideas = ARG_COAL_INFRA
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Preliminary_excavation_studies"
			set_country_flag = COAL_SUCCES
			remove_ideas = ARG_COAL_INFRA
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Purchase_German_Railways = {

		icon = ger_mefo_bills

		allowed = {
			original_tag = ARG
		}

		targets = { GER }

		target_trigger = {
			always = yes
		}

		available = {
			NOT = {
				OR = {
					has_war_with = GER
					GER = {
						exists = no
					}
				}
			}
		}

		visible = {
			NOT = {
				OR = {
					has_war_with = GER
					GER = {
						exists = no
					}
				}
			}
		}

		modifier = {
			civilian_factory_use = 2
		}

		fire_only_once = yes

		cost = 120

		days_remove = 180

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Purchase_German_Railways target: [From.GetName]"
			if = {
				limit = {
					has_idea = ARG_Railway1
				}
				swap_ideas = {
					remove_idea = ARG_Railway1
					add_idea = ARG_Railway3
				}
				else = {
					add_ideas = ARG_Railway2
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Purchase_German_Railways target: [From.GetName]"
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 7
				num_of_factories > 3
			}
			modifier = {
				has_war = no
				factor = 3
			}
		}
	}

	ARG_Purchase_Canadian_Railways = {

		icon = ger_mefo_bills

		allowed = {
			original_tag = ARG
		}

		targets = { CAN }

		target_trigger = {
			always = yes
		}

		available = {
			NOT = {
				OR = {
					has_war_with = CAN
					CAN = {
						exists = no
					}
				}
			}
		}

		visible = {
			NOT = {
				OR = {
					has_war_with = CAN
					CAN = {
						exists = no
					}
				}
			}
		}

		modifier = {
			civilian_factory_use = 2
		}

		fire_only_once = yes

		cost = 120

		days_remove = 180

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Purchase_Canadian_Railways target: [From.GetName]"
			if = {
				limit = {
					has_idea = ARG_Railway2
				}
				swap_ideas = {
					remove_idea = ARG_Railway2
					add_idea = ARG_Railway3
				}
				else = {
					add_ideas = ARG_Railway1
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Purchase_Canadian_Railways target: [From.GetName]"
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 7
				num_of_factories > 3
			}
			modifier = {
				has_war = no
				factor = 3
			}
		}
	}

	ARG_Seize_German_Railways = {

		icon = ger_mefo_bills

		allowed = {
			original_tag = ARG
		}

		targets = { GER }

		target_trigger = {
			always = yes
		}

		available = {
			OR = {
				has_war_with = GER
				GER = {
					exists = no
				}
			}
		}

		visible = {
			OR = {
				has_war_with = GER
				GER = {
					exists = no
				}
			}
		}

		modifier = {
			civilian_factory_use = 2
		}

		fire_only_once = yes

		cost = 60

		days_remove = 180

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Seize_German_Railways target: [From.GetName]"
			if = {
				limit = {
					has_idea = ARG_Railway1
				}
				swap_ideas = {
					remove_idea = ARG_Railway1
					add_idea = ARG_Railway3
				}
				else = {
					add_ideas = ARG_Railway2
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Seize_German_Railways target: [From.GetName]"
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 7
				num_of_factories > 3
			}
			modifier = {
				has_war = no
				factor = 3
			}
		}
	}

	ARG_Seize_Canadian_Railways = {

		icon = ger_mefo_bills

		allowed = {
			original_tag = ARG
		}

		targets = { CAN }

		target_trigger = {
			always = yes
		}

		available = {
			OR = {
				has_war_with = CAN
				CAN = {
					exists = no
				}
			}
		}

		visible = {
			OR = {
				has_war_with = CAN
				CAN = {
					exists = no
				}
			}
		}

		modifier = {
			civilian_factory_use = 2
		}

		fire_only_once = yes

		cost = 60

		days_remove = 180

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Seize_Canadian_Railways target: [From.GetName]"
			if = {
				limit = {
					has_idea = ARG_Railway2
				}
				swap_ideas = {
					remove_idea = ARG_Railway2
					add_idea = ARG_Railway3
				}
				else = {
					add_ideas = ARG_Railway1
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Seize_Canadian_Railways target: [From.GetName]"
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 7
				num_of_factories > 3
			}
			modifier = {
				has_war = no
				factor = 3
			}
		}
	}
}

ARG_diplomacy_decisions = {
	ARG_Joining_the_Entente = {

		icon = generic_nationalism

		target_root_trigger = {
			has_country_flag = ARG_CAN_DEAL
		}

		targets = { ENT }

		target_trigger = {
			FROM = {
				is_faction_leader = yes
			}
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			is_in_faction = no
			is_subject = no
			OR = {
				has_war = no
				custom_trigger_tooltip = {
					tooltip = GRE_same_wars_as_entente
					has_war = yes
					NOT = {
						any_enemy_country = {
							NOT = { has_war_with = FROM }
						}
					}
				}
			}
		}

		visible = {
			has_country_flag = ARG_CAN_DEAL
			FROM = {
				is_faction_leader = yes
			}
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Joining_the_Entente target: [From.GetName]"
			FROM = {
				country_event = argfor.92
			}
		}

		ai_will_do = {
			factor = 5
			modifier = {
				factor = 0
				has_game_rule = {
					rule = ARG_foreign_policy
					option = ARG_FOREIGN_POLICY_NEUTRAl
				}
			}
			modifier = {
				factor = 0
				# we have some warring to do first
				has_authoritarian_government = yes
				NOT = { has_country_flag = GOU_Control }
				OR = {
					AND = {
						NOT = { has_completed_focus = ARG_Punish_Paraguay }
						can_declare_war_on = PAR
					}
					AND = {
						NOT = { has_completed_focus = ARG_Uruguay_is_ours }
						can_declare_war_on = URG
					}
					AND = {
						NOT = { has_completed_focus = ARG_Yacuiba_Dispute }
						can_declare_war_on = BOL
						BOL = {
							owns_state = 848
						}
					}
					AND = {
						NOT = { has_completed_focus = ARG_Yacuiba_Dispute }
						can_declare_war_on = PRU
						PRU = {
							owns_state = 848
						}
					}
					AND = {
						NOT = { has_completed_focus = ARG_Ultimate_Confrontation }
						can_declare_war_on = BRA
					}
				}
			}
		}
	}

	ARG_Joining_the_Reichspakt = {

		icon = generic_nationalism

		target_root_trigger = {
			has_country_flag = ARG_GER_DEAL
		}

		targets = { GER }

		target_trigger = {
			FROM = {
				is_faction_leader = yes
			}
		}

		allowed = {
			original_tag = ARG
		}

		available = {
			is_in_faction = no
			is_subject = no
			OR = {
				has_war = no
				custom_trigger_tooltip = {
					tooltip = GRE_same_wars_as_germany
					has_war = yes
					NOT = {
						any_enemy_country = {
							NOT = { has_war_with = GER }
						}
					}
				}
			}
		}

		visible = {
			has_country_flag = ARG_GER_DEAL
			FROM = {
				is_faction_leader = yes
			}
		}

		fire_only_once = yes

		cost = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Joining_the_Reichspakt target: [From.GetName]"
			GER = {
				country_event = argfor.91
			}
		}

		ai_will_do = {
			factor = 1000
			modifier = {
				factor = 0
				has_game_rule = {
					rule = ARG_foreign_policy
					option = ARG_FOREIGN_POLICY_NEUTRAl
				}
			}
			modifier = {
				factor = 0
				# we have some warring to do first
				has_authoritarian_government = yes
				NOT = { has_country_flag = GOU_Control }
				OR = {
					AND = {
						NOT = { has_completed_focus = ARG_Punish_Paraguay }
						can_declare_war_on = PAR
					}
					AND = {
						NOT = { has_completed_focus = ARG_Uruguay_is_ours }
						can_declare_war_on = URG
					}
					AND = {
						NOT = { has_completed_focus = ARG_Yacuiba_Dispute }
						can_declare_war_on = BOL
						BOL = {
							owns_state = 848
						}
					}
					AND = {
						NOT = { has_completed_focus = ARG_Yacuiba_Dispute }
						can_declare_war_on = PRU
						PRU = {
							owns_state = 848
						}
					}
					AND = {
						NOT = { has_completed_focus = ARG_Ultimate_Confrontation }
						can_declare_war_on = BRA
					}
				}
			}
		}
	}

	ARG_Pressuring_Uruguay = {

		icon = generic_prepare_civil_war

		allowed = {
			original_tag = ARG
		}

		available = {
			ROOT = {
				is_subject = no
				has_authoritarian_government = yes
				OR = {
					is_in_faction = no
					is_faction_leader = yes
				}
			}
			can_declare_war_on = URG
			URG = {
				is_subject = no
			}
			BRA = {
				OR = {
					has_dictatorship_government = yes
					has_socialist_government = yes
				}
			}
		}

		visible = {
			country_exists = URG
		}

		fire_only_once = yes

		cost = 50

		ai_will_do = {
			factor = 50
			modifier = {
				factor = 10
				BRA = { has_war = yes }
			}
			modifier = {
				factor = 0
				URG = { is_guaranteed_by = BRA }
				BRA = { is_in_faction = yes }
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Pressuring_Uruguay"
			URG = {
				country_event = argfor.14
			}
		}
	}

	ARG_Integrate_Uruguay = {
		icon = generic_decision

		allowed = {
			original_tag = ARG
		}

		visible = {
			has_country_flag = GOU_Control
			NOT = { has_government = national_populist }
			owns_state = FROM
			FROM = {
				NOT = { is_core_of = ROOT }
			}
		}

		cost = 100
		days_remove = 300
		fire_only_once = yes
		cancel_if_not_visible = yes
		
		state_target = yes
		targets = { 300 }
		target_root_trigger = {
			has_country_flag = GOU_Control
			NOT = { has_government = national_populist }
		}
		target_trigger = {
			owns_state = FROM
			FROM = {
				NOT = { is_core_of = ROOT }
			}
		}
		
		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancelled ARG_Integrate_Uruguay"
			add_political_power = 100
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Integrate_Uruguay"
			add_state_core = FROM
		}

		ai_will_do = {
			factor = 1000
		}
	}
}

ARG_companies_decisions = {
	ARG_Belgian_Artillery = {

		icon = ger_military_buildup

		allowed = {
			original_tag = ARG
		}

		visible = {
			NOT = {
				has_idea = argentina_frc
			}
		}

		fire_only_once = yes

		cost = 75

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Belgian_Artillery"
			set_country_flag = ARG_FRC
			add_ideas = argentina_frc
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_German_Guns = {

		icon = generic_industry

		allowed = {
			original_tag = ARG
		}

		visible = {
			NOT = {
				has_idea = argentina_mauser
			}
		}

		fire_only_once = yes

		cost = 75

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_German_Guns"
			set_country_flag = ARG_Mauser
			add_ideas = argentina_mauser
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_German_Tanks = {

		icon = generic_tank

		allowed = {
			original_tag = ARG
		}

		visible = {
			NOT = {
				has_idea = argentina_dwm
			}
		}

		fire_only_once = yes

		cost = 75

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_German_Tanks"
			set_country_flag = ARG_BMW
			add_ideas = argentina_dwm
		}

		ai_will_do = {
			factor = 5
		}
	}

	ARG_Dutch_Planes = {

		icon = generic_air

		allowed = {
			original_tag = ARG
		}

		visible = {
			NOT = {
				has_idea = argentina_fokker
			}
		}

		fire_only_once = yes

		cost = 75

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision ARG_Dutch_Planes"
			set_country_flag = ARG_Fokker
			add_ideas = argentina_fokker
		}

		ai_will_do = {
			factor = 5
		}
	}
}

ARG_Buenos_Aires_Lima_axis_decisions = {
	ARG_dismantle_natpop_faction = {
		icon = GFX_decision_freedom
		allowed = { always = no }
		visible = {
			is_faction_leader = yes
		}

		cost = 0
		fire_only_once = yes
		ai_will_do = { base = 0 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_dismantle_natpop_faction"
			dismantle_faction = yes
		}
	}
	ARG_Axis_invite_to_research_group = {
		icon = GFX_decision_generic_research

		visible = {
			FROM = {
				is_in_faction_with = ROOT
			}
		}

		target_array = global.south_american_tags_array
		target_root_trigger = {
			has_completed_focus = ARG_Joint_Military_Developement
		}
		target_trigger = {
			FROM = { is_in_faction_with = ROOT }
		}

		cost = 15
		fire_only_once = yes
		cancel_if_not_visible = yes

		ai_will_do = { base = 75 }

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Axis_invite_to_research_group target: [From.GetName]"
			FROM = {
				add_to_tech_sharing_group = argentina_research
				add_tech_bonus = {
					name = ARG_Joint_Military_Developement
					category = infantry_weapons
					category = artillery
					bonus = 0.33
					uses = 2
				}
			}
		}
	}
	ARG_Axis_subjugate_south_america_decision = {
		icon = GFX_decision_oppression

		visible = {
			can_declare_war_on = FROM
		}

		target_array = global.south_american_tags_array
		target_root_trigger = {
			has_completed_focus = ARG_Subjugate_South_America
		}
		target_trigger = {
			can_declare_war_on = FROM
		}

		cost = 20
		days_remove = 30
		fire_only_once = yes
		cancel_if_not_visible = yes
		war_with_target_on_remove = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				OR = {
					ROOT_is_threatened_cautious = yes
					FROM = { default_attack_AI_trigger_FROM = yes }
					FROM = { dont_start_major_faction_wars = yes }
					NOT = {
						OR = {
							is_neighbor_of = FROM
							any_allied_country = { is_neighbor_of = FROM }
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel ARG_Axis_subjugate_south_america_decision target: [From.GetName]"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Axis_subjugate_south_america_decision target: [From.GetName]"
			FROM = { country_event = kr.political.30 } #warning event
			setup_decision_attack_AI = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Axis_subjugate_south_america_decision target: [From.GetName]"
			clear_decision_attack_AI = yes
			if = {
				limit = {
					FROM = {
						is_subject = no
						is_in_faction = no
						has_socialist_government = no
					}
				}
				FROM = { country_event = argfor.400 }
			}
			else = {
				declare_war_on = {
					target = FROM
					type = puppet_wargoal_focus
				}
			}
		}
	}
	ARG_Axis_secure_central_america_decision = {
		icon = GFX_decision_eng_puppet_usa

		visible = {
			can_declare_war_on = FROM
			FROM = { has_socialist_government = yes }
		}

		target_array = global.central_american_tags_array
		target_root_trigger = {
			has_completed_focus = ARG_Secure_The_Frontier
		}
		target_trigger = {
			can_declare_war_on = FROM
			FROM = { has_socialist_government = yes }
		}

		cost = 20
		days_remove = 30
		fire_only_once = yes
		cancel_if_not_visible = yes
		war_with_target_on_remove = yes

		ai_will_do = {
			base = 100
			modifier = {
				factor = 0
				OR = {
					ROOT_is_threatened_cautious = yes
					FROM = { default_attack_AI_trigger_FROM = yes }
					FROM = { dont_start_major_faction_wars = yes }
					NOT = {
						OR = {
							is_neighbor_of = FROM
							any_allied_country = { is_neighbor_of = FROM }
						}
					}
				}
			}
		}

		cancel_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision cancel ARG_Axis_secure_central_america_decision target: [From.GetName]"
			clear_decision_attack_AI = yes
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete ARG_Axis_secure_central_america_decision target: [From.GetName]"
			FROM = { country_event = kr.political.30 } #warning event
			setup_decision_attack_AI = yes
		}

		remove_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision remove ARG_Axis_secure_central_america_decision target: [From.GetName]"
			clear_decision_attack_AI = yes
			declare_war_on = {
				target = FROM
				type = topple_government
			}
		}
	}
}


