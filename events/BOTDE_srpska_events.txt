add_namespace = sksdbg
add_namespace = sksbts
add_namespace = sks

country_event = { #debug
	id = sksdbg.0
	title = sksdbg.0.t
	desc = sksdbg.0.d
	
	picture = GFX_report_event_BAN_big_speech2

	fire_only_once = yes

	is_triggered_only = yes

	option = { #Egads!
		name = sksdbg.0.a
		set_variable = {
			Nigeria_German_Authority = 0
		}
	}
}

country_event = { #Destabilizing Austria - Allow Entrenched Paramilitary
	id = sksbts.0
	title = sksbts.0.t
	desc = sksbts.0.d
	
	picture = GFX_report_event_BAN_big_speech2

	fire_only_once = no
	
	hidden = yes

	is_triggered_only = yes

	option = { #Egads!
		name = sksbts.0.a
		if = {
			limit = {
				OR = {
					has_dynamic_modifier = {
						modifier = CRO_chetnik_presence
					}
					has_dynamic_modifier = {
						modifier = HUN_cross_brotherhood_presence
					}
					has_dynamic_modifier = {
						modifier = GAL_lancieri_presence
					}
					has_dynamic_modifier = {
						modifier = HUN_indrazneti_presence
					}
				}
			}
			#set_state_flag = can_be_entrenched
		}
	}
}

country_event = { #debug
	id = sks.0
	title = sks.0.t
	desc = sks.0.d
	
	picture = GFX_report_event_BAN_big_speech2

	fire_only_once = yes

	is_triggered_only = yes

	option = { #Egads!
		name = sks.0.a
		set_variable = {
			Nigeria_German_Authority = 0
		}
	}
}